# Intro API Canvas 🎨

Este proyecto es una práctica inicial para comprender el funcionamiento de la **API de Canvas de HTML5**, el renderizado de gráficos 2D mediante JavaScript y la integración de estilos avanzados con CSS.

## 📋 Descripción
El programa consiste en una interfaz web que inicializa un lienzo dinámico ajustable al tamaño del visor (viewport). En él, se renderiza una forma geométrica con propiedades personalizadas como bordes redondeados, sombras y estilos de color específicos (relleno rojo claro y contorno azul).

## 🚀 Características Técnicas
* **Dimensiones Dinámicas:** El canvas se ajusta automáticamente al 50% del ancho y alto de la pantalla del usuario.
* **Estilizado CSS3:** Uso de `border-radius` y `box-shadow` para mejorar la estética del contenedor.
* **Manipulación del DOM:** Selección y configuración del contexto de renderizado `2d` mediante JavaScript.
* **Control de Versiones:** Proyecto gestionado íntegramente con Git.



## 🛠️ Tecnologías Utilizadas
* **HTML5:** Estructura del lienzo.
* **CSS3:** Diseño, bordes redondeados y efectos de sombra.
* **JavaScript (Vanilla):** Lógica de dibujo y cálculos de dimensiones.
* **Git:** Control de versiones.

## 📁 Estructura del Proyecto
```text
intro-api-canvas/
├── index.html      # Estructura principal
├── estilos.css     # Estilos visuales del canvas
├── script.js       # Lógica de dibujo y renderizado
└── README.md       # Documentación del proyecto